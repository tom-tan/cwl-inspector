sudo: required

notifications:
  email: false

language: python
python:
  - 3.6

services:
  - docker

install:
  - pip install flake8 pydocstyle

script:
  - make lint

after_success:
  - ./deploy.sh
